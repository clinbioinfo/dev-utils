<snippet>
	<content><![CDATA[
my \$logger = ${1:DevelopmentUtils}::Logger::getInstance(
	log_level => \$loglevel,
	log_file  => \$logfile
	);

if (!defined(\$logger)){
	confess ("Could not instantiate ${1:DevelopmentUtils}::Logger");
}

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>logger-get-instance</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.perl</scope>
</snippet>
