<snippet>
	<content><![CDATA[

my \$$1 = $2::getInstance(@_);
if (!defined(\$$1)){
	\$self->{_logger}->logconfess("Could not instantiate $2");
}

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>get-instance</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.perl</scope>
</snippet>
